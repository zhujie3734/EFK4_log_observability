agent_name: example-monitoring-agent
check_interval_seconds: 60

graylog:
  enabled: true
  host: graylog.example.internal
  port: 12201

containers:
  - name: example-container
    auto_restart: true
    cooldown_minutes: 30
