services:
  sre-agent:
    build: .
    image: monitor-agent:0.1.0
    container_name: monitor-agent
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - TZ=Europe/London